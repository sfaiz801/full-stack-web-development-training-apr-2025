export const metadata = {
  title: "Authentication | Classic Tailors",
  description: "Sign up or sign in to your Classic Tailors account",
};

export default function AuthLayout({ children }) {
  return <>{children}</>;
}
